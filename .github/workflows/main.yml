on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: tauri-build
        uses: JonasKruckenberg/tauri-build@v1.0.3
        with:
          projectPath: './QPC-Application'

        
              